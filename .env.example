# ============================================
# CONFIGURACIÓN DEL SISTEMA DE GESTIÓN DE EVENTOS
# ESPOL - SOFG1006
# ============================================

# ============================================
# CONFIGURACIÓN DE AZURE SQL DATABASE
# ============================================
# Servidor de Azure SQL Database (ejemplo: tu-servidor.database.windows.net)
DB_SERVER=tu-servidor.database.windows.net

# Nombre de la base de datos
DB_DATABASE=eventos_db

# Usuario administrador de la base de datos
DB_USER=admin_eventos

# Contraseña del usuario (NUNCA COMPARTIR EN REPOSITORIO)
DB_PASSWORD=tu_password_seguro_aqui

# Puerto de conexión (por defecto SQL Server usa 1433)
DB_PORT=1433

# ============================================
# CONFIGURACIÓN DEL SERVIDOR
# ============================================
# Puerto en el que correrá el servidor (Azure App Service usa process.env.PORT)
PORT=3000

# Entorno de ejecución (development, production, test)
NODE_ENV=development

# ============================================
# CONFIGURACIÓN DE CORS
# ============================================
# Orígenes permitidos separados por comas
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://tu-frontend.azurewebsites.net

# ============================================
# CONFIGURACIÓN DE SEGURIDAD
# ============================================
# Clave secreta para JWT (si se implementa autenticación)
JWT_SECRET=tu_clave_secreta_para_jwt_aqui

# Tiempo de expiración del token (ejemplo: 24h, 7d, 30d)
JWT_EXPIRATION=24h

# ============================================
# CONFIGURACIÓN DE LA APLICACIÓN
# ============================================
# Nombre de la aplicación
APP_NAME=Sistema de Gestión de Eventos ESPOL

# URL base de la API
API_BASE_URL=http://localhost:3000/api/v1

# ============================================
# CONFIGURACIÓN DE LOGS
# ============================================
# Nivel de logs (error, warn, info, debug)
LOG_LEVEL=info

# ============================================
# NOTAS IMPORTANTES
# ============================================
# 1. Copiar este archivo como .env y rellenar con valores reales
# 2. NUNCA subir el archivo .env al repositorio (está en .gitignore)
# 3. En Azure App Service, configurar estas variables en "Configuration" > "Application settings"
# 4. Asegurarse de agregar la IP del App Service al firewall de Azure SQL Database
